

import Foundation

// A structure for sending a message. 

struct Message {
    let sender: String // Going to contain the email address of the person who sent the message
    let body: String // Going to contain the body of the message.
}
